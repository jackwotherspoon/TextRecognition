# TextRecognition
Basic code to implement Tesseract-OCR and OpenCV to preprocess and extract text from an image.
Note: Project has dependencies such as Tesseract and OpenCV libraries that need to be downloaded.
